import { Routes } from '@angular/router';
import { FeedbackDialogComponent } from './feedback-dialog.component';

export const feedbackDialogRoutes: Routes = [
    {
        path: 'feedback-dialog',
        component: FeedbackDialogComponent
    }
];
